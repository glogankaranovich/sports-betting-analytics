[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Exclude ops directory from test discovery and coverage
norecursedirs = ops .git __pycache__ .pytest_cache
addopts = 
    -v
    --strict-markers
    --tb=short
    --cov-fail-under=80
    --cov-config=.coveragerc
    --ignore=ops
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    aws: Tests that require AWS resources
    readonly: Integration tests that only read data (safe to run anytime)
    writes: Integration tests that write test data (includes cleanup)
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
